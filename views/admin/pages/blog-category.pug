extends ../layouts/default.pug

block main
  .card-header(bis_skin_checked="1")
    .row.align-items-center.mb-1(bis_skin_checked="1")
      .col(bis_skin_checked="1")
        h4.card-title #{pageTitle}
      // end col
      .col-auto(bis_skin_checked="1")
          a.btn.btn-danger.me-1(
            href=`/${pathAdmin}/blog/category/trash`
            )
            i.far.fa-trash-alt.me-1
            | Thùng rác
          a.btn.btn-primary(
            href=`/${pathAdmin}/blog/category/create`
            )
            i.fa-solid.fa-plus.me-1
            | Tạo danh mục
      // end col
    // end row

  .card-body.pt-0(bis_skin_checked="1")
    //- Search
    .row
      .col-lg-6
        form.input-group.mb-3(form-search)
          input.form-control(
            placeholder="Từ khóa..."
            type="text"
            name="keyword"
            )
          button.btn.btn-secondary Tìm kiếm
    //- Table
    .table-responsive(bis_skin_checked="1")
      table.table.table-striped.mb-0
        thead.table-light
          tr
            th STT
            th Avatar
            th Tên danh mục
            th Danh mục cha
            th Trạng thái
            th Lượt xem
            th.text-end Hành động
        tbody
          each item, index in categoryList
            tr
              td #{pagination.skip+index+1}
              td
                if(item.avatar)
                  img(
                    src=`${domainCDN}${item.avatar}`
                    class="rounded thumb-md me-1 d-inline"
                  )
              td #{item.name}
              td #{item.parentName}
              td
                if (item.status == "active")
                  span.badge.bg-primary Hoạt động
                else if(item.status == "inactive")
                  span.badge.bg-danger Tạm dừng
              td #{item.view}
              td.text-end
                a(href=`/${pathAdmin}/blog/category/edit/${item.id}`)
                  i.las.la-pen.text-secondary.font-16.fs-20
                a(
                  href="javascript:;"
                  button-api
                  data-method="PATCH"
                  data-api=`/${pathAdmin}/blog/category/delete/${item.id}`
                  )
                  i.las.la-trash-alt.text-secondary.font-16.fs-20
      // end /table
    //- Pagination
    .row.mt-3.mb-2
      .col-lg-2.col-md-3.col-6
        select.form-select(pagination)
          - for(let i = 1; i <= pagination.totalPage; i++)
            option(value=i) Trang #{i}
